{
  "name": "quickstart",
  "version": "1.0.0",
  "description": "Hugo static site with vendored Bootstrap 5.3.8",
  "scripts": {
    "dev": "hugo server -D",
    "build": "hugo --gc --minify"
  },
  "keywords": ["hugo", "bootstrap", "static-site", "template"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "bootstrap": "5.3.8"
  },
  "optionalDependencies": {},
  "notes": {
    "bootstrap": "Bootstrap 5.3.8 is vendored in assets/vendor/bootstrap/ - npm install is NOT required for building this project",
    "npm": "This package.json is for documentation purposes only. The project works without node_modules."
  }
}
